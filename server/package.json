{
  "name": "vehicle-matcher-server",
  "version": "0.1.0",
  "description": "Matching-engine API for Find My Perfect BMW — keeps the dataset and weights off the client",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  }
}
