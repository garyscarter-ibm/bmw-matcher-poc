# Handover — 2026-07-21

Where the `feat/question-set-fixes` branch stands, so it can be picked up cold.

## TL;DR

Branch **`feat/question-set-fixes`**, 8 commits ahead of `main`, **working
tree clean, 47/47 tests passing, nothing pushed and no PR open.** Everything
below is committed. The one thing needing a *human* decision before more code
is the range-data source (see "Waiting on you" §3).

## What the branch contains (oldest → newest)

| Commit | What |
|---|---|
| `6c3513a` | Audit harness (`npm run audit`) + findings doc + the "no runtime pruning" decision |
| `e09dcf2` | Fix 1 — drop the `boot` question, derive its need from `people`+`primaryUse` |
| `fd8eeec` | Fix 2 — bind BMW to the named body style (body weight 2.5→4.5, miss→0) |
| `82bd400` | Fix 3 — unmet-want note in results (the replacement for pruning) |
| `bac83ab` | Bug — results headline said "BMW" for MINI users; now per-brand |
| `04e74d1` | Proposal doc for the MINI-first question set |
| `c6b0c9b` | MINI-first set built — styleLine/doors questions, drop mileage/style |
| `54911dc` | Re-mapped `fixtures/*-cars.json` to carry the new fields |

## Verified this session

- **All 47 server tests pass** (`cd server && npm test`).
- **Offline audit** (`npm run audit sens`): MINI outcome diversity 37%→47%,
  `miniVibe` sensitivity ~60%→87%, `doors` 40%; **every BMW figure identical**
  (the MINI-only dimensions are inert for BMW — asserted by a same-answers A/B
  in `brand.test.js`).
- **Live in-browser** (MINI, against the real Sytner Luton feed): doors
  question appears for hatchback and is skipped for SUV (count adjusts 9↔8); a
  Sport pick lifts JCW cars; results headline reads "YOUR PERFECT MINI IS…";
  the new reason "Sport trim, just the character you asked for" renders. No
  console or server errors.

## NOT yet verified / loose ends

1. **Fix 3's unmet-want note was not exercised live this session.** It has
   server tests (unmet fuel, unmet body, met want, no-preference) and the
   sub-agent checked it against a stub, but my browser run was a happy path
   with stock available, so the real "all-petrol MINI retailer → EV note"
   path hasn't been eyeballed in the app. Worth a 2-minute check: run the
   quiz asking for a fully-electric MINI at a retailer with no EVs and
   confirm the note appears above the hero in the MINI voice ("SMALL SNAG.").
2. **styleLine parses for only ~53% of MINI stock** — the older "Cooper S 3
   Door" naming states the perf tier, not a style word, so ~47% score neutral
   (never penalised). Working as designed, but if we want the trim question to
   bite harder, improving the parse (or accepting the ceiling) is a follow-up.
   The perf-tier question (Cooper/S/JCW) was **rejected** as redundant — it's a
   price+0-62 ladder the engine already scores. Don't re-add it.
3. **Fixtures were re-mapped offline, not re-fetched.** Same point-in-time
   cars, plus the new fields. If you want genuinely current stock, run
   `node scripts/dump-stock.js all` (hits the live network, few minutes) then
   re-run the audit.

## Waiting on you (decisions, not code)

1. **Push + PR?** The branch is self-contained and green. Nothing is pushed.
   If you want a PR, the 8 commits tell a clean story on their own.
2. **Keep the `bac83ab` headline fix in this branch?** It's a pre-existing bug
   unrelated to the question-set work, isolated in its own commit — easy to
   cherry-pick elsewhere or drop if you'd rather it travel separately.
3. **Range data source** (from the range investigation — see
   `docs/mini-first-questions.md` "What it takes to build" and the agent's
   findings): there is **no sanctioned structured source** for the new-car
   ranges. `www.{bmw,mini}.co.uk` are hard bot-blocked; the UCP configurator
   API has the data but sits behind an explicit "No bots allowed" + a borrowed
   API key (**rejected — don't build it**); the UK VCA "Car Fuel Data" CSV is
   authoritative and clean **but robots-disallowed for automation** — a *human*
   download is fine. If you download it, I'll write the parser/normaliser. Note
   it carries fuel/MPG/CO2/range but **not** boot/seats/0-62, so it validates
   the economy fields, not the `MODEL_SPECS` gaps. The MINI-first work did
   **not** need it — trim and doors came from the used feed's `derivative`.

## Key files & docs

- `docs/question-stock-audit.md` — the audit, the three fixes, the pruning
  decision. `npm run audit [dead|sens|size|all]` re-runs it.
- `docs/mini-first-questions.md` — the MINI-first proposal + "Built +
  validated" section with the numbers and the resolved open questions.
- `server/brands.js` — the per-brand add/drop hook (the divergence mechanism),
  MINI tuning (styleLine/doors weights + configs).
- `server/mapping.js` — `miniStyleLine` / `miniDoors` parsers.
- `server/engine.js` — `scoreStyleLine` / `scoreDoors`; the weight-gating in
  `effectiveWeights` (an unanswered dimension stays inert) and the zero-weight
  skip in `rankCars` (what keeps BMW untouched).

## Stale thing to ignore

The sub-agent left a background-task chip for the "MINI shown a BMW headline"
bug. That's already fixed (`bac83ab`), so the chip is stale — ignore it (it
can't be dismissed programmatically from here).

## How to resume

```sh
cd server && npm test          # expect 47 pass
cd .. && npm run audit sens     # re-confirm the MINI/BMW numbers (~2 min)
# live preview (two servers):
cd server && PORT=8787 node index.js &     # API
npm run serve                              # static, then open:
# http://localhost:3000/index-mini.html?api=http://localhost:8787
```
