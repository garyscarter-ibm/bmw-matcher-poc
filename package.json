{
  "name": "bmw-matcher",
  "version": "0.1.0",
  "description": "Find My Perfect BMW — a UK car-matching quiz built as a portable Adobe EDS block",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "preview": "python3 -m http.server 3000"
  }
}
